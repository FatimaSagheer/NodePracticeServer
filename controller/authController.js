// // authController.js

// const loginController = require('./loginSystem'); // Adjust the path to your login controller
// const abc = require("../models/loginSystem");

// const userAccess = (req, res) => {
//   if (req.user.role === 'user') {
//     // Example: Call the login function from the login controller
//     loginController.loginUser(req, res);
//   } else {
//     res.status(403).json({ message: 'Access denied' });
//   }
// };

// module.exports = {
//   userAccess
// };
